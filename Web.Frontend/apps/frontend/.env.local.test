# Test configuration to demonstrate module isolation
# Copy this to .env.local to test disabling the checklists module

# Disable the checklists module (it has a deliberate error)
NEXT_PUBLIC_ENABLE_CHECKLISTS=false

# Keep other modules enabled
NEXT_PUBLIC_ENABLE_INCIDENTS=true
NEXT_PUBLIC_ENABLE_ASSETS=true
NEXT_PUBLIC_ENABLE_RISK_ASSESSMENT=true