CREATE TABLE [V7].[LocationType] (
    [LocationTypeID] INT           NOT NULL,
    [Reference]      NVARCHAR (50) NOT NULL,
    [Title] NVARCHAR (100) NULL,
    PRIMARY KEY CLUSTERED ([LocationTypeID] ASC)
);

